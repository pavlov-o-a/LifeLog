name: flutter_mixture
description: Flutter mixture project
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^5.0.0
  rxdart: ^0.27.1
  flutter_bloc: ^7.1.0
  cupertino_icons: ^1.0.3
  googleapis_auth: ^1.1.0
  googleapis: ^4.0.0
  webview_flutter: ^2.0.10

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
